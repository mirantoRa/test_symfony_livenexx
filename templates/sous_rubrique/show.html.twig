{% extends 'base.html.twig' %}

{% block title %}SousRubrique{% endblock %}

{% block body %}
    <h1>SousRubrique</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sous_rubrique.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ sous_rubrique.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_sous_rubrique_index') }}">back to list</a>

    <a href="{{ path('app_sous_rubrique_edit', {'id': sous_rubrique.id}) }}">edit</a>

    {{ include('sous_rubrique/_delete_form.html.twig') }}
{% endblock %}

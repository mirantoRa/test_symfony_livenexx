{% extends 'base.html.twig' %}

{% block title %}SousRubrique index{% endblock %}

{% block body %}
    <h1>SousRubrique index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for sous_rubrique in sous_rubriques %}
            <tr>
                <td>{{ sous_rubrique.id }}</td>
                <td>{{ sous_rubrique.name }}</td>
                <td>
                    <a href="{{ path('app_sous_rubrique_show', {'id': sous_rubrique.id}) }}">show</a>
                    <a href="{{ path('app_sous_rubrique_edit', {'id': sous_rubrique.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_sous_rubrique_new') }}">Create new</a>
{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}Edit SousRubrique{% endblock %}

{% block body %}
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="#">Rubriques</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="nav-link" href="{{ path('app_rubrique_index') }}">Rubriques</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('app_sous_rubrique_index') }}">SousRubriques</a>
            </li>
        </ul>
    </div>
</nav>

    <div class="container">
        <h1 class="mt-4 mb-3">Edit SousRubrique</h1>

        {{ include('sous_rubrique/_form.html.twig', {'button_label': 'Modifier'}) }}

        <a href="{{ path('app_sous_rubrique_index') }}" class="btn btn-secondary mt-3">Retour à la liste</a>
    </div>
{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}Rubrique {{ rubrique.name }}{% endblock %}

{% block body %}
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="nav-link" href="{{ path('app_rubrique_index') }}">Rubriques</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('app_sous_rubrique_index') }}">Sous rubriques</a>
            </li>
        </ul>
    </div>
</nav>


    <div class="container">
        <h1 class="mt-4 mb-3">Rubrique :  {{ rubrique.name }}</h1>

        <div class="row">
            <div class="col-md-6">
                <h3 class="mb-3">Sous-Rubriques :</h3>
                <ul class="list-group">
                    {% for sousRubrique in rubrique.sousRubriques %}
                        <li class="list-group-item">{{ sousRubrique.name }}</li>
                    {% else %}
                        <li class="list-group-item">Pas de sous-rubriques disponibles</li>
                    {% endfor %}
                </ul>
            </div>
        </div>
          <a href="{{ path('app_rubrique_index') }}" class="btn btn-secondary mt-3">Retour à la liste</a>
    </div>
    
{% endblock %}
